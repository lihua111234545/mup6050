# --- 1. 首先，忽略所有文件 ---
*

# --- 2. 然后，递归地取消忽略所有目录 (否则Git不会进入任何子目录) ---
!*/

# --- 3. 最后，只**取消忽略**你真正需要跟踪的文件 ---
# 源代码文件
!*.c
!*.h
!*.s          # 启动文件

# 核心配置文件
!.gitignore   
!.mxproject   # CubeMX项目文件 (根据团队需求决定)
!*.ioc        # CubeMX配置文件
!*.uvprojx    # Keil项目主文件 (跟踪此文件以共享项目结构)
!*.sct        # 分散加载文件

# 取消忽略特定目录（即使其中某些文件未匹配，也确保目录本身被跟踪）
!/Core/
!/Core/**
!/Drivers/
!/Drivers/**